# site properties and page defaults
title: Spatial Omics and Bioinformatics Lab
subtitle: Department of Biotechnology, IIT Madras<br>Centre for Integrative Biology and Systems Medicine (IBSE), IIT Madras
description: 
header: images/header.jpg
footer: images/header.jpg
proofer: false

# site social media and other links
links:
  email: agupta@iitm.ac.in
  orcid: 0000-0002-1137-3043
  google-scholar: lvn04sUAAAAJ
  linkedin: anushkag
  twitter: AnushkaG2509

### jekyll settings

# front matter defaults
defaults:
  # all markdown files
  - scope:
      path: ""
    values:
      layout: default
  # markdown files in /_members
  - scope:
      type: "members"
    values:
      layout: member
  # markdown files in /_posts
  - scope:
      type: "posts"
    values:
      layout: post

collections:
  # generate page for each member
  members:
    output: true
  # generate page for each post
  posts:
    output: true

# jekyll plugins
plugins:
  - jekyll-spaceship
  - jekyll-sitemap
  - jekyll-redirect-from
  - jekyll-feed
  - jekyll-last-modified-at

# code block syntax highlighting
highlighter: rouge

# jekyll theme
theme: null

# sass settings
sass:
  sass_dir: _styles

# force jekyll to include certain files/folders
include:
  - _styles
  - _scripts

# force jekyll to exclude certain files/folders
exclude:
  - README.md
  - LICENSE.md
  - CITATION.cff
